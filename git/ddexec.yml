#!/usr/local/bin/ddexec --
git:
  image: git
  working_dir: ${PWD}
  stdin_open: true
  tty: true
  dockerfile: |
    FROM alpine
    RUN apk add --no-cache git
    ENTRYPOINT [ "/usr/bin/git" ]
    CMD [ "--help" ]
